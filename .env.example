# Welco ERP Environment Configuration
# Copy this file to .env and update the values as needed

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL connection string (used by Prisma)
# Default values work with the included docker-compose.yml
DATABASE_URL="postgresql://postgres:welco2026@localhost:5432/welco_erp?schema=public"
DIRECT_URL="postgresql://postgres:welco2026@localhost:5432/welco_erp?schema=public"

# =============================================================================
# AUTHENTICATION (NextAuth.js)
# =============================================================================
# The base URL of your application
NEXTAUTH_URL="http://localhost:3000"

# Secret key for encrypting sessions and tokens
# Generate a new one for production: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-change-this-in-production"

# =============================================================================
# APPLICATION
# =============================================================================
NODE_ENV="development"

# =============================================================================
# PRODUCTION (Docker)
# =============================================================================
# These are used by docker-compose.prod.yml
POSTGRES_USER="postgres"
POSTGRES_PASSWORD="change-this-strong-password"
POSTGRES_DB="welco_erp"

# Example production database connection string (using the postgres service name)
DATABASE_URL="postgresql://postgres:change-this-strong-password@postgres:5432/welco_erp?schema=public"
DIRECT_URL="postgresql://postgres:change-this-strong-password@postgres:5432/welco_erp?schema=public"

# =============================================================================
# ADMIN BOOTSTRAP (one-time, production)
# =============================================================================
# Required to create or reset the initial Admin account
ADMIN_BOOTSTRAP_EMAIL="admin@yourcompany.com"
ADMIN_BOOTSTRAP_PASSWORD="ChangeMe!123"
ADMIN_BOOTSTRAP_NAME="System Admin"
ADMIN_BOOTSTRAP_TOKEN="set-a-long-random-token"
ADMIN_BOOTSTRAP_CONFIRM="set-a-long-random-token"
# Optional:
# ADMIN_BOOTSTRAP_ALLOW_RESET="true"
# ADMIN_BOOTSTRAP_FORCE="true"
